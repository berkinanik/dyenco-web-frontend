export enum Spin {
  Topspin = 'topspin',
  None = 'none',
  Backspin = 'backspin',
}

export enum BallFeedRate {
  Low = 'low',
  Medium = 'medium',
  High = 'high',
}

export enum OperationMode {
  IDLE = 'idle',
  BASIC = 'basic',
  ADVANCED = 'advanced',
  RANDOM = 'random',
}
